{
  "name": "pinol-nica-delivery",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=backend\"",
    "install-all": "npm install --workspaces"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
